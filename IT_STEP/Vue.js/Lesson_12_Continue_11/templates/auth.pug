include head
include nav
include footer
doctype html
html(lang="en")
  head
    +head("E-commerce")
  body 
    +nav
    .container 
        h1.login-header LOG IN PAGE
        form.formauth(action="/auth" method="POST")
            input(type="email" name="username" placeholder="Username")
            input(type="password" name="password" placeholder="Password")
            input(type="submit" value="Sign in")
    +footer 